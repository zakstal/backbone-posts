<h3>Posts</h3>

<ul>
  <% posts.each(function(post){ %>
    <li>
      <a href="#/posts/<%= post.escape("id")%>"><%= post.escape("title") %></a>
      <button class="delete-button" data-id="<%= post.escape("id") %>">Delete</button>
    </li>
  <% }); %>
</ul>